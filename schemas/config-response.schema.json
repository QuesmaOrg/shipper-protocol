{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/QuesmaOrg/shipper-protocol/schemas/config-response.schema.json",
  "title": "POST /v1/config response",
  "description": "Carries one document: the organization's served config. A response the client cannot parse or execute is refused whole, and the cached config keeps collecting.",
  "type": "object",
  "required": ["config", "expires_at"],
  "additionalProperties": false,
  "properties": {
    "config": {
      "description": "The served YAML document, standard base64. The stored bytes are served verbatim and cached verbatim.",
      "$ref": "#/$defs/base64"
    },
    "expires_at": {
      "description": "When this document goes stale: server clock plus the control-plane config TTL. Expiry does not stop collection: the client keeps running on the cached config and stamps config_expired, because source stores are reaped on their own schedules and a halted client loses data permanently.",
      "$ref": "#/$defs/timestamp"
    }
  },

  "$defs": {
    "base64": {
      "type": "string",
      "pattern": "^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$",
      "minLength": 4
    },
    "timestamp": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?(Z|[+-][0-9]{2}:[0-9]{2})$"
    }
  }
}
